<h1> Task List </h1>

<% @tasks.each do |task| %>
<div class="task_wrapper">
  <h2 class="title"><%= link_to task.title, task %></h2>
  <p class="date">Created: <%= task.created_at.strftime("%A, %B %d, %Y")%></p>
</div>
<% end %>

<%= link_to "New Task", new_task_path %>
